# הבאת ברכה — משחקי שפרה ופועה

גיבוי המקורות, קובצי העבודה והמסירה של עשרת חלקי הפרויקט עבור אתר מורה שמיים.

## נקודת המשך — 16.9.2026

סבב בדיקת שימושיות ותיקונים במחשב ובתצוגת טלפון. למעבר האחרון פותחים **מסירה-משחקי-הבאת-ברכה/בדיקה-סופית.html**: קישור לכל חלק, דגשים לבדיקה, סטטוס, הערות והורדת רשימת תיקונים. ההערות נשמרות בדפדפן בלבד.

פירוט התיקונים והבדיקות נמצא ב־**מסמכים/סיכום-בדיקה-סופית.html** ובדוחות JSON הסמוכים אליו. לפני הפרסום נשארו הגהת הספר והניקוד ומעבר במכשירי טלפון פיזיים. הגרסה טרם פורסמה באתר.

התיקונים נבנים מתוך `upgraded/dev/review.py`, `review.js` ו־`review.css`; דף המעבר נוצר באמצעות `review-package.py` כחלק מהאריזה. בדיקות הדפדפן נמצאות ב־`review-browser.cjs`, `review-flows.cjs`, `review-deep.cjs` ו־`review-final.cjs`; הן משתמשות ב־Playwright וב־Edge מקומי, מול שרת בפורט 8765. צילומי הבדיקה הזמניים אינם נכנסים לגיבוי Git.

## מבנה התיקייה

- **מסירה-משחקי-הבאת-ברכה/** — כל קובצי המסירה המסודרים. מתחילים בקובץ **התחילו-כאן.html**.
- **מסירה-משחקי-הבאת-ברכה/משחקים/** — המשחקים המוכנים לשילוב באתר.
- **מסירה-משחקי-הבאת-ברכה/מסמכים/** — פירוט השינויים, הוראות חיבור והערות תוכן.
- **מסירה-משחקי-הבאת-ברכה/טקסטים/** — התוכן לפי חלקים והעמודים המקוריים של הספר.
- **מסירה-משחקי-הבאת-ברכה/אריזות/** — ZIP נפרד לכל חלק.
- **מסירה-משחקי-הבאת-ברכה/ערכת-תצוגה-לאתר/** — עשר תמונות שער, PNG ו־WebP, תיאורים, קהלי יעד ונתוני כרטיסים ב־CSV ו־JSON.
- **upgraded/dev/** — תבניות, הרחבות, בדיקות וסקריפטי הבנייה.
- **upgraded/source-review/** — תיעוד תוכן שנשלף מהמקורות לצורך עבודה ובדיקה.
- קובצי HTML, ZIP ו־DOCX שבתיקייה הראשית — המקורות המקוריים של הפרויקט.

## צפייה מקומית

מתוך התיקייה הראשית, עם Python מותקן:

```sh
python -m http.server 8765 --bind 127.0.0.1
```

פותחים את `http://127.0.0.1:8765/` ונכנסים לתיקיית המסירה ולקובץ `התחילו-כאן.html`.

## בנייה ובדיקות

מריצים מתוך התיקייה הראשית, עם Python ו־Node.js מותקנים:

```sh
python upgraded/dev/build.py
node upgraded/dev/check.cjs
node upgraded/dev/collect-data.cjs
python upgraded/dev/package.py
python upgraded/dev/verify-package.py
```

הבנייה משתמשת בספריות הסטנדרטיות בלבד. בדיקת התמונות ב־verify-package.py משתמשת גם ב־Pillow. הקבצים הסופיים הם HTML/JavaScript ואינם דורשים Python או Node.js בשרת האתר.

## תוכן ושמירה

הסיפורים, החידות, הברכות, המשימות והכרטיסים נשמרו מן המקורות. בדיקות הזהות והתייחסות לסתירות במקור נמצאות במסמכי המסירה. הספר המקורי כולל 40 תמונות שבהן מוטמע המלל.

בסבב השדרוג השני נוספו בקרי ניקוד בחלקים א–ח ותצוגת קריאה נוספת לספר. הנוסחים החדשים והתמלול נמצאים גם ב־`טקסטים/נוסחי-קריאה-נוספים.json`. אותיות הניקוד החדש בתעלומה וב־18 הברכות נבדקות אוטומטית מול המקור. תמלול הספר והניקוד החדש מיועדים להגהת עורכת לפני הפרסום.

תמונות השער מבוססות על חפצים ונוף, בלי אנשים, חיות או סמלים פוליטיים ודתיים זרים. מפרט האיורים נשמר ב־`upgraded/dev/assets/art-direction.md` ובמסמכי המסירה. הקבצים המקוריים נשארו ללא שינוי.

התקדמות השחקנים, שרטוטים ופרטים שהוזנו בטפסים נשמרים בדפדפן המקומי ואינם חלק ממאגר זה. הכלי לאמהות מאפשר להוריד גיבוי אישי בנפרד.

הגרסה לבדיקה זמינה דרך GitHub Pages. בדף `מסירה-משחקי-הבאת-ברכה/בדיקה-סופית.html` אפשר לכתוב הערות לכל משחק, לציין שם ומכשיר, ולשתף או להוריד את רשימת התיקונים. הטיוטות נשמרות מיד במכשיר ומגובות אוטומטית לענן; הערות שפורסמו ותגובות נשמרות ב־Sites. אפשר להוריד מסמך HTML עצמאי הכולל גם טיוטות שלא פורסמו, ולשלוח אותו או להדפיס ל־PDF.


## סבב בדיקה משותפת — מצב המשך 16.9.2026

ממשק סימון פריטים, תגובות ומצבי טיפול נמצא ב־`upgraded/dev/point-review.js`. הוא מופעל במשחקים רק עם `?review=1`; דף הבדיקה מפנה כך לכל עשרת המשחקים. אין להכניס לשירות מפתחות GitHub או סודות לדפדפן.

שירות ההערות נבנה בנפרד בתיקייה המקומית `review-service/` (מוחרגת ממאגר המשחקים, עם מאגר Sites עצמאי). זיהוי Sites: `appgprj_6aaa8348e2cc8191ba25cc2fbd51e2ba`. כתובת השירות: `https://bracha-games-review.marpad990579.chatgpt.site`. השמירה משתמשת ב־D1 ובהעלאות תמונה ל־R2. זהות עריכה אנונימית נשמרת במכשיר, והערות משותפות נשמרות בשרת. שדות שטרם נשלחו הם טיוטות בלבד.

המשתמשת אישרה במפורש גישה לכל בעלת קישור, והגישה הציבורית אושרה ב־Sites. גרסת השירות השנייה כוללת גיבוי טיוטות פרטי לפי זהות המכשיר ושחזור לאחר ניתוק. גרסת השירות השנייה פורסמה בהצלחה. בדיקת שרת חי אימתה הערות ותגובות משותפות בין שתי זהויות, פרטיות של טיוטות וחיבור מהאתר; נתוני הבדיקה הזמניים הוסרו.

`node upgraded/dev/shared-review-test.cjs` בודק שני דפדפנים עצמאיים מול קוד השרת עם SQLite מקומי: הערה משותפת, תגובה, מצב טיפול, הרשאת עריכה, טעינה מחדש, הערה כללית, תמונת canvas ושמירה שנכשלת. תוצאות הבדיקה הן מקומיות ואינן טוענות שהגישה הציבורית נבדקה.


### תיקוני עריכה לחלקים א–ג

הנוסחים שאושרו בהודעת המשתמשת שמורים ב־`upgraded/dev/editorial-feedback.json`. הכותרות וההוראות עודכנו במשחקים, בכרטיסי המסירה ובערכת האתר. בחלק ב׳ נוסף פס סגירה שנשאר נגיש בגלילה; בחלק ג׳ המספרים מסודרים בשורה מימין לשמאל ומחליפים שני חלקים בלחיצה. בדיקת `editorial-test.cjs` עברה ברוחבי 320, 390 ו־1440: כל שמונה פעילויות הספר נסגרו בכל שלוש הרמות, השקט ממשיך אחרי עצירה, והרצף נפתר בכל הרמות. 29 אוספי מקור נשמרו זהים; באוסף GAMES של הספר אושרו רק שינויי הכותרות וההוראות המפורטים בקובץ העריכה. קובצי המקור לא שונו.
